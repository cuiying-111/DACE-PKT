# DACE-PKT
我用DACE和GRU对编程知识追踪建模，非对称双序列
在这个版本里面，我把代码分支的最终输出状态与DACE中的题目序列分支（transformer）的输出的去偏后的知识状态融合在一起，
但只是简单的拼接，结果不如原来我只用DACE的主干对题目序列建模的效果好。
后续，改进---------第一版----------------用门控的方式，控制引入代码的融合方式。
做这个门控融合的修改，只是对GRU和DACE的输出结果先做一个门控融合，之后和下一个题目，一起输入到预测层中。
所以，只对model.py的代码进行了修改。


